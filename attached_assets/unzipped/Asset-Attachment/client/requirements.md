## Packages
react-markdown | Rendering the generated BRD markdown
framer-motion | For smooth transitions and animations
lucide-react | Icons (already in base, but emphasizing need)
date-fns | Date formatting if needed
clsx | Class name utility (already in base)
tailwind-merge | Class name utility (already in base)

## Notes
Supabase URL and Key are placeholders as requested.
Webhooks point to http://localhost:5678 as requested.
